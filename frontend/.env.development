# Used automatically by `npm run dev`.
# Points the dev frontend at the LOCAL backend (uvicorn main:app --reload).
VITE_API_URL=http://127.0.0.1:8000
VITE_TRACEABILITY_ENABLED=false
VITE_PRINT_AGENT_ENABLED=false
